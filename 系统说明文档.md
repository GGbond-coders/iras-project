# 智能简历诊断系统（IRAS）— 系统说明文档

> **版本**：v2.0  
> **更新日期**：2026-05-13  
> **项目仓库**：https://github.com/GGbond-coders/iras-project

---

## 目录

1. [系统概述](#1-系统概述)
2. [系统架构](#2-系统架构)
3. [技术栈](#3-技术栈)
4. [项目结构](#4-项目结构)
5. [数据库设计](#5-数据库设计)
6. [后端设计](#6-后端设计)
   - [启动与配置](#61-启动与配置)
   - [安全与认证](#62-安全与认证)
   - [Controller 层](#63-controller-层)
   - [Service 层](#64-service-层)
   - [数据访问层](#65-数据访问层)
   - [数据传输对象](#66-数据传输对象)
   - [工具类](#67-工具类)
7. [前端设计](#7-前端设计)
   - [入口与全局配置](#71-入口与全局配置)
   - [路由设计](#72-路由设计)
   - [状态管理](#73-状态管理)
   - [API 封装](#74-api-封装)
   - [页面组件](#75-页面组件)
8. [API 接口文档](#8-api-接口文档)
9. [AI 引擎集成](#9-ai-引擎集成)
10. [部署指南](#10-部署指南)
11. [源码解析](#11-源码解析)
    - [11.1 后端源码解析](#111-后端源码解析)
    - [11.2 前端源码解析](#112-前端源码解析)
    - [11.3 完整请求链路源码追踪](#113-完整请求链路源码追踪)
12. [附录](#12-附录)

---

## 1. 系统概述

### 1.1 项目简介

IRAS（Intelligent Resume Analysis System）是一套基于 B/S 架构的**智能简历诊断系统**。系统整合了职位检索、AI 职能画像、智能简历诊断三大核心功能，帮助求职者快速了解自身简历与目标岗位的匹配程度，并获得针对性的改进建议。

### 1.2 核心功能

| 功能模块 | 说明 |
|---------|------|
| **用户认证** | 支持用户名/邮箱登录注册，基于 JWT 的无状态会话管理，Token 有效期 24 小时 |
| **职位检索** | 按职位名称、工作城市、薪资范围进行多字段组合模糊筛选，分页展示结果，支持查看详情 |
| **职能画像** | 输入任意职位名称，AI 自动生成该职位的完整能力画像（硬技能、软技能、工具清单、经验要求、学历要求） |
| **智能诊断** | 上传简历文件（.txt/.pdf/.doc/.docx，最大 10MB），AI 自动分析并与岗位库匹配，生成诊断报告（匹配岗位、匹配分、差距分析、面试建议） |

### 1.3 用户角色

本系统目前面向单一用户角色——**求职者**，通过注册/登录后使用系统全部功能。

---

## 2. 系统架构

### 2.1 整体架构图

```
┌──────────────────────────────────────────────────────────────┐
│                        浏览器 (Client)                        │
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │                   前端 (Vue 3 SPA)                       │ │
│  │         Element Plus + Vue Router + Pinia + Axios        │ │
│  │              http://localhost:5173                        │ │
│  └────────────────────────┬────────────────────────────────┘ │
│                           │ HTTP (Vite Proxy: /iras → 8080)  │
└───────────────────────────┼──────────────────────────────────┘
                            │
┌───────────────────────────▼──────────────────────────────────┐
│                    后端 (Spring Boot)                         │
│                                                              │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │  Spring Security (JWT Filter → 认证链)                   │ │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐              │ │
│  │  │ Auth     │  │ Job      │  │ Dify     │              │ │
│  │  │ Controller│  │ Controller│  │ Controller│              │ │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘              │ │
│  │       │              │              │                    │ │
│  │  ┌────▼─────┐  ┌────▼─────┐  ┌────▼─────┐              │ │
│  │  │ Auth     │  │ Job      │  │ Dify     │              │ │
│  │  │ Service  │  │ Service  │  │ Service  │              │ │
│  │  └────┬─────┘  └────┬─────┘  └────┬─────┘              │ │
│  │       │              │              │                    │ │
│  │  ┌────▼─────┐  ┌────▼─────┐       │                    │ │
│  │  │ User     │  │ Job      │       │                    │ │
│  │  │ Mapper   │  │ Mapper   │       │                    │ │
│  │  └────┬─────┘  └────┬─────┘       │                    │ │
│  └───────┼──────────────┼─────────────┼────────────────────┘ │
│          │              │              │                      │
│     ┌────▼─────┐  ┌────▼─────┐  ┌────▼──────────┐          │
│     │  MySQL   │  │  MySQL   │  │  Dify API     │          │
│     │  user表  │  │ job_info │  │ (Workflow)    │          │
│     │          │  │   表     │  │               │          │
│     └──────────┘  └──────────┘  └───────────────┘          │
│                                                              │
│              http://localhost:8080/iras                       │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 请求处理流程

```
浏览器请求 → Vite Proxy(/iras) → Spring Boot(8080/iras)
                                        │
                                  Security Filter Chain
                                        │
                              JwtAuthenticationFilter
                                  (提取并验证 Token)
                                        │
                              ┌─────────┼─────────┐
                              ▼         ▼         ▼
                         AuthController  JobController  DifyController
                              │         │              │
                         AuthService  JobService   DifyService
                              │         │              │
                         UserMapper  JobMapper    Dify Workflow API
                              │         │
                           MySQL      MySQL
```

---

## 3. 技术栈

### 3.1 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 3.2.5 | 应用框架 |
| Spring Security | (随 Boot) | 安全框架，处理认证与授权 |
| MyBatis | 3.0.3 (Starter) | ORM 框架，SQL 映射 |
| MySQL | 8.0+ | 关系型数据库 |
| JJWT | 0.12.5 | JWT 令牌生成与验证 |
| Lombok | — | 简化 Java 代码（getter/setter/构造器） |
| Hutool | 5.8.27 | Java 工具类库 |
| JDK | 17+ | Java 运行环境 |
| Maven | 3.8+ | 项目构建与依赖管理 |

### 3.2 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue | 3.4.21 | 渐进式 JavaScript 框架 |
| Vue Router | 4.3.0 | 前端路由管理 |
| Pinia | 2.1.7 | 状态管理 |
| Element Plus | 2.6.3 | UI 组件库 |
| Element Plus Icons | 2.3.1 | 图标组件 |
| Axios | 1.6.8 | HTTP 请求库 |
| Marked | 12.0.1 | Markdown 渲染库 |
| Vite | 5.2.8 | 前端构建工具 |
| Node.js | 18+ | 运行环境 |

### 3.3 AI 引擎

| 技术 | 说明 |
|------|------|
| Dify Workflow API | AI 工作流引擎，提供职能画像和简历诊断两个 Workflow |
| API Base URL | `https://api.dify.ai/v1` |
| 调用方式 | HTTP POST，blocking 同步模式 |

---

## 4. 项目结构

```
iras-project/
├── README.md                              # 项目说明文件
├── 系统说明文档.md                          # 本文档
│
├── backend/                               # ============ 后端 ============
│   ├── pom.xml                            # Maven 项目配置与依赖声明
│   └── src/main/
│       ├── java/com/iras/
│       │   ├── IrasApplication.java       # Spring Boot 启动类
│       │   │
│       │   ├── config/                    # 配置层
│       │   │   ├── JwtAuthenticationFilter.java  # JWT 认证过滤器（每次请求拦截验证 Token）
│       │   │   └── SecurityConfig.java           # Spring Security 全局配置（CORS、权限规则、异常处理）
│       │   │
│       │   ├── controller/                # 控制器层（REST API 入口）
│       │   │   ├── AuthController.java    # 认证接口：注册、登录
│       │   │   ├── DifyController.java    # AI 接口：职能画像、简历诊断
│       │   │   └── JobController.java     # 职位接口：搜索、详情
│       │   │
│       │   ├── dto/                       # 数据传输对象（请求/响应封装）
│       │   │   ├── Result.java            # 统一响应体（code + message + data）
│       │   │   ├── LoginRequest.java      # 登录请求参数
│       │   │   ├── RegisterRequest.java   # 注册请求参数
│       │   │   ├── JobSearchRequest.java  # 职位搜索请求参数（含分页）
│       │   │   ├── JobProfileRequest.java # 职能画像请求参数
│       │   │   └── PageResult.java        # 分页结果封装
│       │   │
│       │   ├── entity/                    # 实体类（对应数据库表）
│       │   │   ├── User.java              # 用户实体
│       │   │   └── JobInfo.java           # 职位信息实体
│       │   │
│       │   ├── mapper/                    # 数据访问层（MyBatis Mapper 接口）
│       │   │   ├── UserMapper.java        # 用户 CRUD
│       │   │   └── JobMapper.java         # 职位 CRUD + 多条件搜索
│       │   │
│       │   ├── service/                   # 业务逻辑层
│       │   │   ├── AuthService.java       # 认证服务接口
│       │   │   ├── DifyService.java       # Dify AI 服务接口
│       │   │   ├── JobService.java        # 职位服务接口
│       │   │   └── impl/
│       │   │       ├── AuthServiceImpl.java   # 认证服务实现（注册/登录/密码加密/JWT 生成）
│       │   │       ├── DifyServiceImpl.java   # Dify 服务实现（HTTP 调用 Dify Workflow API）
│       │   │       └── JobServiceImpl.java    # 职位服务实现（搜索/分页/详情）
│       │   │
│       │   └── util/                      # 工具类
│       │       └── JwtUtil.java           # JWT 工具（生成/解析/验证 Token）
│       │
│       └── resources/
│           ├── application.yml            # 应用配置文件（端口、数据库、JWT、Dify）
│           ├── init.sql                   # 数据库初始化脚本（建表 + 示例数据）
│           └── mapper/
│               └── JobMapper.xml          # MyBatis SQL 映射文件（动态查询）
│
└── frontend/                              # ============ 前端 ============
    ├── index.html                         # HTML 入口文件
    ├── package.json                       # NPM 依赖与脚本配置
    ├── vite.config.js                     # Vite 配置（含代理规则）
    ├── public/                            # 静态资源
    │   ├── favicon.svg                    # SVG 格式图标
    │   ├── favicon16.png                  # 16x16 PNG 图标
    │   └── favicon32.png                  # 32x32 PNG 图标
    └── src/
        ├── main.js                        # Vue 应用入口（注册插件：Pinia、Router、Element Plus）
        ├── App.vue                        # 根组件（全局样式 + router-view）
        │
        ├── api/
        │   └── index.js                   # Axios 实例 + 请求/响应拦截器 + API 封装
        │
        ├── router/
        │   └── index.js                   # 路由配置 + 路由守卫（登录鉴权）
        │
        ├── store/
        │   └── user.js                    # Pinia 用户状态管理（Token、用户信息持久化）
        │
        └── views/                         # 页面组件
            ├── Login.vue                  # 登录/注册页面（Tab 切换）
            ├── Layout.vue                 # 主布局（侧边栏导航 + 面包屑 + 用户下拉菜单）
            ├── Jobs.vue                   # 职位检索页面（搜索表单 + 结果表格 + 详情弹窗）
            ├── JobProfile.vue             # 职能画像页面（输入职位名 → AI 分析 → 展示画像）
            └── Diagnosis.vue              # 智能诊断页面（上传简历 → AI 诊断 → 匹配报告）
```

---

## 5. 数据库设计

### 5.1 数据库信息

- **数据库名**：`iras`
- **字符集**：`utf8mb4`
- **排序规则**：`utf8mb4_unicode_ci`

### 5.2 表结构

#### 5.2.1 用户表（`user`）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | `bigint` | PRIMARY KEY, AUTO_INCREMENT | 用户唯一标识 |
| `username` | `varchar(50)` | NOT NULL, UNIQUE | 用户名（登录凭据） |
| `password` | `varchar(255)` | NOT NULL | 密码（BCrypt 加密存储） |
| `email` | `varchar(100)` | DEFAULT NULL | 联系邮箱（可选，可用于登录） |
| `create_time` | `datetime` | DEFAULT CURRENT_TIMESTAMP | 注册时间 |

**索引：**
- `uk_username` — 唯一索引，用户名

#### 5.2.2 职位库表（`job_info`）

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| `id` | `bigint` | PRIMARY KEY, AUTO_INCREMENT | 职位唯一标识 |
| `job_name` | `varchar(100)` | NOT NULL | 职位名称 |
| `company_name` | `varchar(100)` | NOT NULL | 公司名称 |
| `city` | `varchar(50)` | DEFAULT NULL | 工作城市 |
| `salary` | `varchar(50)` | DEFAULT NULL | 薪资范围 |
| `jd_text` | `text` | NOT NULL | 职位描述（JD 内容） |
| `type` | `varchar(50)` | DEFAULT NULL | 职位分类（全职/实习/校招） |
| `create_time` | `datetime` | DEFAULT CURRENT_TIMESTAMP | 数据导入时间 |
| `update_time` | `datetime` | ON UPDATE CURRENT_TIMESTAMP | 最后更新时间 |

**索引：**
- `idx_job_name` — 普通索引，职位名称
- `idx_city` — 普通索引，城市
- `idx_fulltext_jd` — 全文索引，职位描述

### 5.3 示例数据

系统预置了 8 条职位示例数据，覆盖以下岗位：

| 职位 | 公司 | 城市 | 薪资 |
|------|------|------|------|
| Java后台开发工程师 | 上海联泉智能科技有限公司 | 上海 | 15000 |
| 前端开发工程师 | 北京字节跳动科技有限公司 | 北京 | 20000 |
| Python测试开发工程师 | 深圳腾讯计算机系统有限公司 | 深圳 | 18000 |
| Android开发工程师 | 杭州阿里巴巴网络科技有限公司 | 杭州 | 22000 |
| iOS开发工程师 | 北京小米科技有限公司 | 北京 | 20000 |
| 后端开发工程师 | 上海拼多多信息技术有限公司 | 上海 | 25000 |
| 数据分析师 | 广州网易计算机系统有限公司 | 广州 | 15000 |
| 算法工程师 | 北京百度网讯科技有限公司 | 北京 | 30000 |

---

## 6. 后端设计

### 6.1 启动与配置

#### 6.1.1 启动类 `IrasApplication`

- 使用 `@SpringBootApplication` 标记为 Spring Boot 应用
- 使用 `@MapperScan("com.iras.mapper")` 扫描 MyBatis Mapper 接口

#### 6.1.2 应用配置 `application.yml`

| 配置项 | 值 | 说明 |
|--------|-----|------|
| `server.port` | `8080` | 服务端口 |
| `server.servlet.context-path` | `/iras` | 应用上下文路径 |
| `spring.datasource.url` | `jdbc:mysql://localhost:3306/iras` | 数据库连接地址 |
| `spring.datasource.username` | `root` | 数据库用户名 |
| `spring.datasource.password` | `1234` | 数据库密码 |
| `spring.servlet.multipart.max-file-size` | `10MB` | 单文件上传大小限制 |
| `spring.servlet.multipart.max-request-size` | `10MB` | 请求总大小限制 |
| `mybatis.mapper-locations` | `classpath:mapper/*.xml` | MyBatis XML 映射文件位置 |
| `mybatis.configuration.map-underscore-to-camel-case` | `true` | 自动驼峰命名转换 |
| `jwt.secret` | `IRAS-Secret-Key-...` | JWT 签名密钥（HS512） |
| `jwt.expiration` | `86400000` | Token 有效期（24 小时，单位毫秒） |
| `dify.base-url` | `https://api.dify.ai/v1` | Dify API 基础地址 |
| `dify.job-profile-key` | `app-1S7R9zv...` | 职能画像 Workflow API Key |
| `dify.resume-diagnosis-key` | `app-eAFPOpK...` | 简历诊断 Workflow API Key |
| `logging.level.com.iras` | `debug` | 日志级别 |

---

### 6.2 安全与认证

#### 6.2.1 SecurityConfig — Spring Security 全局配置

**核心配置：**
- **CSRF**：禁用（REST API 无状态，不需要 CSRF 保护）
- **CORS**：允许所有来源（`*`），支持 `GET/POST/PUT/DELETE/OPTIONS`，缓存 1 小时
- **Session**：无状态（`STATELESS`），不使用 HttpSession
- **权限规则**：
  - `/api/auth/**` — 允许匿名访问（登录、注册）
  - `OPTIONS /**` — 允许预检请求
  - 其他所有请求 — 需要认证
- **异常处理**：未认证请求返回 401 JSON 响应 `{"code":401,"message":"未登录或Token已过期"}`
- **过滤器顺序**：`JwtAuthenticationFilter` 在 `UsernamePasswordAuthenticationFilter` 之前执行

**Bean 注册：**
- `PasswordEncoder` — BCrypt 密码编码器
- `AuthenticationManager` — 认证管理器
- `CorsConfigurationSource` — CORS 配置源

#### 6.2.2 JwtAuthenticationFilter — JWT 认证过滤器

**处理流程：**

```
请求进入
    │
    ▼
从 Header 提取 Token
(格式: "Bearer <token>")
    │
    ▼
Token 有效？
    ├─ 是 → 解析用户名 → 设置 SecurityContext 认证信息
    └─ 否 → 跳过认证（后续由 SecurityConfig 决定是否放行）
    │
    ▼
继续过滤器链
```

**关键方法：**
- `doFilterInternal()` — 每次请求执行的主过滤逻辑
- `extractToken()` — 从 `Authorization` 请求头提取 Bearer Token

#### 6.2.3 JwtUtil — JWT 工具类

**功能：**

| 方法 | 说明 |
|------|------|
| `generateToken(username)` | 生成 JWT Token，包含 subject（用户名）、签发时间、过期时间 |
| `getUsernameFromToken(token)` | 从 Token 中解析出用户名（subject） |
| `validateToken(token)` | 验证 Token 是否有效（签名正确 + 未过期） |

**签名算法：** HMAC-SHA512（HS512），密钥从配置文件读取

---

### 6.3 Controller 层

#### 6.3.1 AuthController — 认证控制器

| 端点 | 方法 | 说明 |
|------|------|------|
| `POST /api/auth/register` | `register()` | 用户注册（参数校验：用户名+密码非空） |
| `POST /api/auth/login` | `login()` | 用户登录（参数校验：用户名+密码非空） |

**请求/响应示例：**

```json
// POST /api/auth/register
// Request:
{ "username": "testuser", "password": "123456", "email": "test@example.com" }
// Response:
{ "code": 200, "message": "注册成功", "data": { "token": "eyJ...", "username": "testuser", "email": "test@example.com" } }

// POST /api/auth/login
// Request:
{ "username": "testuser", "password": "123456" }
// Response:
{ "code": 200, "message": "登录成功", "data": { "token": "eyJ...", "username": "testuser", "email": "test@example.com" } }
```

#### 6.3.2 JobController — 职位控制器

| 端点 | 方法 | 说明 |
|------|------|------|
| `GET /api/jobs/search` | `searchJobs()` | 职位搜索（支持 jobName、city、salaryMin、salaryMax、page、size） |
| `GET /api/jobs/{id}` | `getJobById()` | 获取职位详情（按 ID 查询） |

**请求/响应示例：**

```
GET /api/jobs/search?jobName=开发&city=北京&page=1&size=20

Response:
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [ { "id": 1, "jobName": "...", "companyName": "...", ... } ],
    "total": 8,
    "page": 1,
    "size": 20,
    "totalPages": 1
  }
}
```

#### 6.3.3 DifyController — AI 功能控制器

| 端点 | 方法 | 说明 |
|------|------|------|
| `POST /api/dify/job-profile` | `getJobProfile()` | 职能画像（JSON body: `{"job_name":"软件工程师"}`） |
| `POST /api/dify/diagnose` | `diagnoseResume()` | 简历诊断（multipart/form-data，字段名: `file`） |

**错误码：**
- `400` — 参数为空
- `500` — AI 服务调用失败

---

### 6.4 Service 层

#### 6.4.1 AuthService / AuthServiceImpl — 认证服务

**注册流程：**

```
接收注册请求
    │
    ▼
用户名已存在？ ──是──→ 返回错误 "用户名已存在"
    │否
    ▼
邮箱已被注册？ ──是──→ 返回错误 "邮箱已被注册"
    │否
    ▼
BCrypt 加密密码
    │
    ▼
插入数据库
    │
    ▼
生成 JWT Token
    │
    ▼
返回 Token + 用户信息
```

**登录流程：**

```
接收登录请求
    │
    ▼
按用户名查找用户
    │
未找到 → 按邮箱查找用户
    │
仍未找到 → 返回错误 "用户名或密码错误"
    │
    ▼
BCrypt 验证密码
    │
不匹配 → 返回错误 "用户名或密码错误"
    │
    ▼
生成 JWT Token
    │
    ▼
返回 Token + 用户信息
```

**特点：**
- 支持用户名或邮箱两种方式登录
- 密码使用 BCrypt 加密存储
- 登录成功/注册成功均返回 JWT Token

#### 6.4.2 JobService / JobServiceImpl — 职位服务

**搜索逻辑：**
1. 计算分页偏移量 `offset = (page - 1) * size`
2. 调用 Mapper 执行多条件模糊搜索
3. 调用 Mapper 统计匹配总数
4. 封装为 `PageResult` 返回（含 records、total、page、size、totalPages）

**默认分页参数：** page=1, size=20

#### 6.4.3 DifyService / DifyServiceImpl — AI 服务

**职能画像调用流程：**

```
接收职位名称
    │
    ▼
构建 Workflow 请求体
(inputs: { job_name: "..." })
    │
    ▼
POST https://api.dify.ai/v1/workflows/run
(Header: Bearer <job-profile-key>)
    │
    ▼
解析响应 → 返回 result 字段
```

**简历诊断调用流程：**

```
接收简历文件
    │
    ▼
第一步：上传文件到 Dify
POST /v1/files/upload
(multipart/form-data: file + user)
    │
    ▼
获取 upload_file_id
    │
    ▼
第二步：调用 Workflow
POST /v1/workflows/run
(inputs: { resume_text: [{transfer_method, upload_file_id, type}] })
    │
    ▼
解析响应 → 返回 result 字段
```

**技术细节：**
- 使用 `HttpURLConnection` 直接发起 HTTP 请求（未使用 RestTemplate/WebClient）
- 连接超时：文件上传 60 秒，Workflow 调用 300 秒（5 分钟）
- 文件上传使用手动拼接 multipart/form-data 请求体
- 请求模式：`blocking`（同步阻塞等待 AI 推理完成）

---

### 6.5 数据访问层

#### 6.5.1 UserMapper

| 方法 | SQL | 说明 |
|------|-----|------|
| `findByUsername(username)` | `SELECT * FROM user WHERE username = ?` | 按用户名查找 |
| `findByEmail(email)` | `SELECT * FROM user WHERE email = ?` | 按邮箱查找 |
| `findById(id)` | `SELECT * FROM user WHERE id = ?` | 按 ID 查找 |
| `insert(user)` | `INSERT INTO user (username, password, email) VALUES (?, ?, ?)` | 插入新用户（自增主键回填） |

#### 6.5.2 JobMapper

| 方法 | 类型 | 说明 |
|------|------|------|
| `searchJobs(jobName, city, salaryMin, salaryMax, offset, size)` | XML 动态 SQL | 多字段模糊搜索 + 分页 |
| `countSearchJobs(jobName, city, salaryMin, salaryMax)` | XML 动态 SQL | 统计搜索结果总数 |
| `findById(id)` | 注解 `@Select` | 按 ID 查询职位 |
| `findAll()` | 注解 `@Select` | 查询所有职位（供 AI 诊断匹配） |

**动态 SQL 特性（JobMapper.xml）：**
- 使用 `<where>` + `<if>` 实现条件拼接
- 职位名称、城市：`LIKE CONCAT('%', #{value}, '%')` 模糊匹配
- 薪资范围：`CAST(REPLACE(REPLACE(salary, 'k', ''), 'K', '') AS UNSIGNED)` 转数字比较
- 排序：`ORDER BY id DESC`
- 分页：`LIMIT #{offset}, #{size}`

---

### 6.6 数据传输对象

| 类名 | 用途 | 关键字段 |
|------|------|---------|
| `Result<T>` | 统一 API 响应封装 | `code`(int), `message`(String), `data`(T) |
| `LoginRequest` | 登录请求 | `username`(@NotBlank), `password`(@NotBlank) |
| `RegisterRequest` | 注册请求 | `username`(@NotBlank), `password`(@NotBlank), `email` |
| `JobSearchRequest` | 职位搜索请求 | `jobName`, `city`, `salaryMin`, `salaryMax`, `page`(默认1), `size`(默认20) |
| `JobProfileRequest` | 职能画像请求 | `jobName` |
| `PageResult<T>` | 分页结果 | `records`(List), `total`(long), `page`(int), `size`(int), `totalPages`(int) |

**`Result<T>` 工厂方法：**
- `Result.success(data)` — 200 + "success"
- `Result.success(message, data)` — 200 + 自定义消息
- `Result.error(code, message)` — 自定义错误码 + 消息
- `Result.error(message)` — 500 + 消息

---

### 6.7 工具类

#### JwtUtil

- **生成 Token**：使用 `Jwts.builder()` 设置 subject、签发时间、过期时间，HS512 签名
- **解析 Token**：使用 `Jwts.parser()` 验证签名并提取 claims 中的 subject
- **验证 Token**：尝试解析，成功返回 `true`，任何异常返回 `false`

---

## 7. 前端设计

### 7.1 入口与全局配置

#### 7.1.1 `index.html`

- 字符集：UTF-8
- 响应式视口：`width=device-width, initial-scale=1.0`
- Favicon：SVG + PNG 多格式兼容
- 页面标题：`智能简历诊断系统`

#### 7.1.2 `main.js`

Vue 应用初始化，按顺序注册：
1. **Pinia** — 状态管理
2. **Vue Router** — 路由
3. **Element Plus** — UI 组件库（中文语言包 `zh-cn`）
4. **Element Plus Icons** — 全量注册所有图标组件

#### 7.1.3 `App.vue`

- 根组件，仅包含 `<router-view />`
- 全局样式重置（`margin: 0; padding: 0; box-sizing: border-box`）
- 字体：`PingFang SC`, `Microsoft YaHei`, `sans-serif`
- 背景色：`#f5f7fa`

#### 7.1.4 `vite.config.js`

- 插件：`@vitejs/plugin-vue`
- 开发服务器端口：`5173`
- 代理规则：`/iras` → `http://localhost:8080`（解决跨域问题）

---

### 7.2 路由设计

```
/login          → Login.vue         (无需认证)
/               → Layout.vue        (需认证，重定向到 /jobs)
  /jobs         → Jobs.vue          (职位检索)
  /job-profile  → JobProfile.vue    (职能画像)
  /diagnosis    → Diagnosis.vue     (智能诊断)
```

**路由守卫逻辑：**
- 目标路由 `requiresAuth !== false` 且用户未登录（`token` 为空）→ 重定向到 `/login`
- 目标路由是 `/login` 且用户已登录 → 重定向到 `/`
- 其他情况 → 正常放行

**页面缓存：** 使用 `<keep-alive>` 缓存已访问的路由组件，避免重复渲染。

---

### 7.3 状态管理

#### `user.js` (Pinia Store)

**状态：**
- `token` — JWT Token（从 localStorage 持久化）
- `username` — 用户名（从 localStorage 持久化）
- `email` — 邮箱（从 localStorage 持久化）

**方法：**
- `setUser(userData)` — 保存用户信息到 state + localStorage
- `logout()` — 清空 state + localStorage

---

### 7.4 API 封装

#### `api/index.js`

**Axios 实例配置：**
- `baseURL`: `/iras`（通过 Vite 代理转发到后端）
- `timeout`: 300000ms（5 分钟，适配 Dify AI 推理耗时）
- 默认 `Content-Type`: `application/json`

**请求拦截器：**
- 自动从 localStorage 读取 Token 并添加到 `Authorization: Bearer <token>` 请求头

**响应拦截器：**
- 成功响应：检查业务码 `code`，非 200 弹出错误提示
- 401 错误：清除本地存储，跳转到登录页，提示"登录已过期"
- 其他错误：弹出错误消息

**API 模块：**

| 模块 | 方法 | 调用 |
|------|------|------|
| `authApi` | `register(data)` | `POST /api/auth/register` |
| `authApi` | `login(data)` | `POST /api/auth/login` |
| `jobApi` | `search(params)` | `GET /api/jobs/search` |
| `jobApi` | `getById(id)` | `GET /api/jobs/{id}` |
| `difyApi` | `getJobProfile(jobName)` | `POST /api/dify/job-profile` |
| `difyApi` | `diagnoseResume(file)` | `POST /api/dify/diagnose` (multipart) |

---

### 7.5 页面组件

#### 7.5.1 Login.vue — 登录/注册页

**功能：**
- Tab 切换登录/注册表单
- 登录表单：用户名/邮箱 + 密码
- 注册表单：用户名 + 邮箱（选填）+ 密码 + 确认密码
- 表单验证：必填校验、密码长度（≥6）、确认密码一致性
- 登录/注册成功后自动保存用户信息并跳转到首页

**UI 设计：**
- 渐变紫色背景（`#667eea` → `#764ba2`）
- 白色卡片居中布局，圆角 16px，阴影效果

#### 7.5.2 Layout.vue — 主布局

**布局结构：**
```
┌──────────┬──────────────────────────────────┐
│  侧边栏  │            主内容区              │
│          │  ┌────────────────────────────┐  │
│  IRAS    │  │  面包屑导航 + 用户下拉菜单  │  │
│          │  ├────────────────────────────┤  │
│  职位检索 │  │                            │  │
│  职能画像 │  │       <router-view />       │  │
│  智能诊断 │  │       (keep-alive)         │  │
│          │  │                            │  │
│          │  └────────────────────────────┘  │
└──────────┴──────────────────────────────────┘
```

**功能：**
- 侧边栏可折叠（点击 IRAS 标题切换）
- 侧边栏宽度：展开 220px / 折叠 64px
- 深色主题侧边栏（`#1e293b`）
- 面包屑导航显示当前页面
- 用户下拉菜单支持退出登录

#### 7.5.3 Jobs.vue — 职位检索页

**功能：**
- 搜索表单：职位名称、工作城市、薪资范围（最低/最高）
- 结果表格：职位名称、公司名称、城市、薪资（红色高亮）、类型（Tag 标签）、操作按钮
- 分页组件：支持切换每页条数（20/50/100）、页码跳转
- 详情弹窗：展示完整职位信息（使用 `el-descriptions` 组件）

**数据流：**
```
页面加载 → onMounted → fetchJobs() → jobApi.search() → 渲染表格
搜索按钮 → handleSearch() → page=1 → fetchJobs()
重置按钮 → 清空表单 → fetchJobs()
```

#### 7.5.4 JobProfile.vue — 职能画像页

**功能：**
- 输入框：输入职位名称，支持回车触发分析
- 加载状态：显示"AI 正在深度分析职位画像..."
- 结果展示：四宫格卡片布局
  - 硬技能要求（列表）
  - 软技能要求（列表）
  - 常用工具（Tag 标签）
  - 经验与学历（文本）

**数据流：**
```
输入职位名 → 点击"开始分析"
    │
    ▼
difyApi.getJobProfile(jobName)
    │
    ▼
解析返回的 JSON 字符串
(字段: job_title, hard_skills, soft_skills, tools, experience, education)
    │
    ▼
渲染四宫格卡片
```

#### 7.5.5 Diagnosis.vue — 智能诊断页

**功能：**
- 文件上传：拖拽或点击上传，支持 .txt/.pdf/.doc/.docx（最大 10MB）
- 已选文件显示文件名、大小，支持"重新上传"
- 加载状态：显示"AI 正在深度分析您的简历..."
- 诊断结果：
  - 匹配岗位列表（卡片式）
  - 每个岗位展示：匹配分（颜色编码）、公司/城市/类型 Tag、薪资、匹配原因、差距分析、面试建议
  - 推理过程（可折叠显示 `<think>` 标签内容）
  - 纯文本结果的 Markdown 渲染

**匹配分颜色编码：**
- ≥ 80%：绿色（`#67c23a`）
- 60%-79%：橙色（`#e6a23c`）
- < 60%：红色（`#f56c6c`）

**数据流：**
```
选择文件 → handleFileChange() → selectedFile
点击"开始诊断"
    │
    ▼
difyApi.diagnoseResume(file)
    │
    ▼
提取 <think> 标签内容（推理过程）
    │
    ▼
尝试解析 JSON
    ├─ 数组 → matches（结构化匹配结果）
    ├─ 对象含 matches 字段 → matches
    ├─ 对象含 result 字段 → 递归解析
    └─ 解析失败 → rawResult（Markdown 渲染）
    │
    ▼
渲染匹配卡片列表 / Markdown 报告
```

---

## 8. API 接口文档

### 8.1 认证接口

#### POST `/iras/api/auth/register`

**功能：** 用户注册

**请求体：**
```json
{
  "username": "string (必填, 2-20字符)",
  "password": "string (必填, ≥6字符)",
  "email": "string (选填)"
}
```

**成功响应：**
```json
{
  "code": 200,
  "message": "注册成功",
  "data": {
    "token": "eyJhbGciOiJIUzUxMiJ9...",
    "username": "testuser",
    "email": "test@example.com"
  }
}
```

**失败响应：**
```json
{ "code": 400, "message": "用户名已存在", "data": null }
{ "code": 400, "message": "邮箱已被注册", "data": null }
```

---

#### POST `/iras/api/auth/login`

**功能：** 用户登录（支持用户名或邮箱）

**请求体：**
```json
{
  "username": "string (用户名或邮箱)",
  "password": "string"
}
```

**成功响应：**
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzUxMiJ9...",
    "username": "testuser",
    "email": "test@example.com"
  }
}
```

**失败响应：**
```json
{ "code": 401, "message": "用户名或密码错误", "data": null }
```

---

### 8.2 职位接口

> ⚠️ 以下接口需要在请求头中携带 `Authorization: Bearer <token>`

#### GET `/iras/api/jobs/search`

**功能：** 职位搜索（多字段模糊匹配 + 分页）

**Query 参数：**

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `jobName` | String | 否 | 职位名称（模糊匹配） |
| `city` | String | 否 | 工作城市（模糊匹配） |
| `salaryMin` | String | 否 | 最低薪资 |
| `salaryMax` | String | 否 | 最高薪资 |
| `page` | Integer | 否 | 页码（默认 1） |
| `size` | Integer | 否 | 每页条数（默认 20，可选 50/100） |

**响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": {
    "records": [
      {
        "id": 1,
        "jobName": "Java后台开发工程师",
        "companyName": "上海联泉智能科技有限公司",
        "city": "上海",
        "salary": "15000",
        "jdText": "5年及以上 本科 Java...",
        "type": "全职",
        "createTime": "2024-01-01 00:00:00",
        "updateTime": "2024-01-01 00:00:00"
      }
    ],
    "total": 8,
    "page": 1,
    "size": 20,
    "totalPages": 1
  }
}
```

---

#### GET `/iras/api/jobs/{id}`

**功能：** 获取职位详情

**路径参数：** `id` (Long) — 职位 ID

**成功响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": { "id": 1, "jobName": "...", ... }
}
```

**失败响应：**
```json
{ "code": 404, "message": "职位不存在", "data": null }
```

---

### 8.3 AI 接口

> ⚠️ 以下接口需要在请求头中携带 `Authorization: Bearer <token>`

#### POST `/iras/api/dify/job-profile`

**功能：** 职能画像分析

**请求体：**
```json
{
  "job_name": "软件工程师"
}
```

**响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": "{\"job_title\":\"软件工程师\",\"hard_skills\":[\"...\"],\"soft_skills\":[\"...\"],\"tools\":[\"...\"],\"experience\":\"...\",\"education\":\"...\"}"
}
```

> 注：`data` 字段为 JSON 字符串，前端需要 `JSON.parse()` 解析

---

#### POST `/iras/api/dify/diagnose`

**功能：** 简历智能诊断

**请求格式：** `multipart/form-data`

| 字段 | 类型 | 说明 |
|------|------|------|
| `file` | File | 简历文件（.txt/.pdf/.doc/.docx，最大 10MB） |

**响应：**
```json
{
  "code": 200,
  "message": "success",
  "data": "[{\"matched_job\":\"Java开发工程师\",\"matched_score\":85,\"matched_reason\":\"...\",\"gap_points\":[\"...\"],\"interview_advice\":[\"...\"]}]"
}
```

> 注：`data` 字段可能为 JSON 数组字符串、Markdown 文本，或包含 `<think>` 推理标签的混合内容

---

## 9. AI 引擎集成

### 9.1 Dify Workflow API

本系统通过 Dify 平台的 Workflow API 实现两个 AI 功能：

| 功能 | API Key | 输入 | 输出 |
|------|---------|------|------|
| 职能画像 | `dify.job-profile-key` | 职位名称（文本） | 能力画像 JSON |
| 简历诊断 | `dify.resume-diagnosis-key` | 简历文件（文档） | 匹配报告 JSON/文本 |

### 9.2 API 调用规范

**基础地址：** `https://api.dify.ai/v1`

**认证方式：** `Authorization: Bearer <api-key>`

**请求格式：**

```json
{
  "inputs": {
    "<key>": "<value>"  // 或文件引用数组
  },
  "response_mode": "blocking",
  "user": "iras-user"
}
```

**文件引用格式（简历诊断）：**

```json
{
  "inputs": {
    "resume_text": [
      {
        "transfer_method": "local_file",
        "upload_file_id": "<上传后获取的文件ID>",
        "type": "document"
      }
    ]
  }
}
```

**响应解析：**
- 成功响应：`data.outputs.result` 字段包含 AI 生成的结果
- 结果可能为 JSON 字符串或纯文本/Markdown

### 9.3 超时设置

| 操作 | 超时时间 |
|------|---------|
| 文件上传到 Dify | 60 秒 |
| Workflow 调用 | 300 秒（5 分钟） |
| 前端 Axios 请求 | 300 秒（5 分钟） |

---

## 10. 部署指南

### 10.1 环境要求

| 环境 | 版本要求 |
|------|---------|
| JDK | 17+ |
| Node.js | 18+ |
| MySQL | 8.0+ |
| Maven | 3.8+ |

### 10.2 部署步骤

#### 1. 数据库初始化

```bash
mysql -u root -p < backend/src/main/resources/init.sql
```

#### 2. 修改后端配置

编辑 `backend/src/main/resources/application.yml`：
- 修改数据库连接信息（`spring.datasource.*`）
- 修改 JWT 密钥（`jwt.secret`，生产环境建议使用强密钥）
- 修改 Dify API Key（`dify.job-profile-key`、`dify.resume-diagnosis-key`）

#### 3. 启动后端

```bash
cd backend
mvn spring-boot:run
```

后端运行在 `http://localhost:8080/iras`

#### 4. 启动前端

```bash
cd frontend
npm install
npm run dev
```

前端运行在 `http://localhost:5173`

#### 5. 生产构建

```bash
cd frontend
npm run build
```

构建产物输出到 `frontend/dist/`，可部署到 Nginx 等 Web 服务器。

### 10.3 生产环境注意事项

- 使用 HTTPS
- 更换 JWT 密钥为高强度随机密钥
- 配置数据库连接池
- 配置 Nginx 反向代理
- 使用 `npm run build` 构建前端静态文件
- 将 Dify API Key 存储为环境变量而非配置文件

---

## 11. 源码解析

> 本章对系统中每一个源码文件进行逐行级别的详细解析，帮助开发者深入理解代码实现细节。

---

### 11.1 后端源码解析

---

#### 11.1.1 IrasApplication.java — Spring Boot 启动类

**文件路径：** `backend/src/main/java/com/iras/IrasApplication.java`

**职责：** 整个应用的入口点，负责启动 Spring Boot 容器。

```java
@SpringBootApplication        // 组合注解 = @Configuration + @EnableAutoConfiguration + @ComponentScan
@MapperScan("com.iras.mapper") // 自动扫描 com.iras.mapper 包下所有 Mapper 接口，注册为 Spring Bean
public class IrasApplication {
    public static void main(String[] args) {
        SpringApplication.run(IrasApplication.class, args);
        // SpringApplication.run() 做了以下事情：
        // 1. 创建 Spring 应用上下文（ApplicationContext）
        // 2. 执行自动配置（根据 classpath 依赖自动装配 Bean）
        // 3. 扫描并注册所有 @Component/@Service/@Controller 等组件
        // 4. 启动内嵌 Web 服务器（Tomcat，默认端口 8080）
    }
}
```

**关键注解说明：**

| 注解 | 作用 |
|------|------|
| `@SpringBootApplication` | 组合注解，启用自动配置、组件扫描、配置类 |
| `@MapperScan("com.iras.mapper")` | 指定 MyBatis Mapper 接口的包路径，自动为每个接口生成代理实现类并注册为 Bean |

---

#### 11.1.2 SecurityConfig.java — Spring Security 安全配置

**文件路径：** `backend/src/main/java/com/iras/config/SecurityConfig.java`

**职责：** 定义全局安全策略，包括 CSRF、CORS、会话管理、接口权限、异常处理、过滤器顺序。

**核心 Bean 详解：**

**① SecurityFilterChain — 安全过滤器链**

```java
@Bean
public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
    http
        .csrf(AbstractHttpConfigurer::disable)    // 禁用 CSRF。原因：REST API 使用 JWT 无状态认证，
                                                   // 不依赖 Cookie，无需 CSRF 保护

        .cors(cors -> cors.configurationSource(corsConfigurationSource()))
                                                   // 启用 CORS，使用自定义配置源

        .sessionManagement(session ->
            session.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
                                                   // 无状态会话：不创建 HttpSession，
                                                   // 每次请求都通过 JWT 重新认证

        .authorizeHttpRequests(auth -> auth
            .requestMatchers("/api/auth/**").permitAll()
                                                   // 放行 /api/auth/* 下所有接口（登录、注册）
            .requestMatchers(HttpMethod.OPTIONS, "/**").permitAll()
                                                   // 放行所有 OPTIONS 预检请求（CORS 规范要求）
            .anyRequest().authenticated()          // 其余所有接口必须经过认证
        )

        .exceptionHandling(ex -> ex
            .authenticationEntryPoint((request, response, authException) -> {
                // 自定义 401 响应：返回 JSON 而非默认的 HTML 登录页
                response.setStatus(HttpServletResponse.SC_UNAUTHORIZED);
                response.setContentType(MediaType.APPLICATION_JSON_VALUE);
                response.setCharacterEncoding("UTF-8");
                objectMapper.writeValue(response.getWriter(), Result.error(401, "未登录或Token已过期"));
            })
        )

        .addFilterBefore(jwtAuthenticationFilter, UsernamePasswordAuthenticationFilter.class);
        // 将 JWT 过滤器插入到 UsernamePasswordAuthenticationFilter 之前
        // 确保在 Spring Security 默认认证流程之前完成 JWT 解析

    return http.build();
}
```

**② PasswordEncoder — 密码编码器**

```java
@Bean
public PasswordEncoder passwordEncoder() {
    return new BCryptPasswordEncoder();
    // BCrypt 特点：
    // - 每次加密结果不同（内置随机盐值）
    // - 相同密码加密后的哈希值不同，防止彩虹表攻击
    // - 验证时使用 passwordEncoder.matches(明文, 密文) 比较
}
```

**③ CorsConfigurationSource — CORS 跨域配置**

```java
@Bean
public CorsConfigurationSource corsConfigurationSource() {
    CorsConfiguration config = new CorsConfiguration();
    config.setAllowedOriginPatterns(List.of("*"));         // 允许所有来源（生产环境应限制）
    config.setAllowedMethods(List.of("GET", "POST", "PUT", "DELETE", "OPTIONS"));
    config.setAllowedHeaders(List.of("*"));                // 允许所有请求头
    config.setAllowCredentials(true);                      // 允许携带 Cookie/凭证
    config.setMaxAge(3600L);                               // 预检请求缓存 1 小时

    UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
    source.registerCorsConfiguration("/**", config);       // 对所有路径生效
    return source;
}
```

---

#### 11.1.3 JwtAuthenticationFilter.java — JWT 认证过滤器

**文件路径：** `backend/src/main/java/com/iras/config/JwtAuthenticationFilter.java`

**职责：** 拦截每个 HTTP 请求，提取并验证 JWT Token，设置认证信息到 SecurityContext。

**执行流程图：**

```
HTTP 请求进入
    │
    ▼
doFilterInternal() 被调用
    │
    ▼
extractToken(request) ─── 从 Header 提取 Token
    │                      格式: "Authorization: Bearer eyJ..."
    │                      substring(7) 去掉 "Bearer " 前缀
    ▼
Token 非空且有效？
    ├─ 是 → jwtUtil.getUsernameFromToken(token) 解析用户名
    │       创建 UsernamePasswordAuthenticationToken
    │       (principal=username, credentials=null, authorities=[])
    │       存入 SecurityContextHolder.getContext().setAuthentication(...)
    │       └─ 后续可通过 SecurityContextHolder 获取当前登录用户
    └─ 否 → 跳过认证设置（SecurityContext 中无认证信息）
    │
    ▼
filterChain.doFilter(request, response) → 继续后续过滤器链
```

**关键代码解析：**

```java
@Override
protected void doFilterInternal(HttpServletRequest request,
                                HttpServletResponse response,
                                FilterChain filterChain) throws ServletException, IOException {
    // 第一步：从请求头提取 JWT Token
    String token = extractToken(request);

    // 第二步：验证 Token 有效性（签名正确 + 未过期）
    if (StringUtils.hasText(token) && jwtUtil.validateToken(token)) {
        // 第三步：从 Token 中解析用户名（即 Token 的 subject 字段）
        String username = jwtUtil.getUsernameFromToken(token);

        // 第四步：创建 Spring Security 认证令牌
        // 参数说明：
        //   - principal (username): 当前认证用户的身份标识
        //   - credentials (null): 不需要密码（已通过 JWT 认证）
        //   - authorities ([]): 暂未设置角色权限
        UsernamePasswordAuthenticationToken authentication =
                new UsernamePasswordAuthenticationToken(username, null, Collections.emptyList());

        // 第五步：将认证信息存入 SecurityContext
        // 后续的 Controller 中可通过 @AuthenticationPrincipal 或
        // SecurityContextHolder.getContext().getAuthentication() 获取当前用户
        SecurityContextHolder.getContext().setAuthentication(authentication);
    }

    // 无论认证是否成功，都继续执行后续过滤器
    // 如果接口需要认证但 Token 无效，SecurityConfig 中的权限规则会拒绝访问
    filterChain.doFilter(request, response);
}

private String extractToken(HttpServletRequest request) {
    String bearerToken = request.getHeader("Authorization");
    // 检查格式: "Bearer <token>"
    if (StringUtils.hasText(bearerToken) && bearerToken.startsWith("Bearer ")) {
        return bearerToken.substring(7);  // 截取 "Bearer " 之后的部分
    }
    return null;  // 无 Token 或格式不正确
}
```

---

#### 11.1.4 JwtUtil.java — JWT 工具类

**文件路径：** `backend/src/main/java/com/iras/util/JwtUtil.java`

**职责：** 封装 JWT 的生成、解析、验证逻辑。

**Token 结构：**

```
eyJhbGciOiJIUzUxMiJ9.eyJzdWIiOiJ0ZXN0dXNlciIsImlhdCI6MTcxMzAwMDAwMCwiZXhwIjoxNzEzMDg2NDAwfQ.xxxxx
│←──────── Header ────────→│←──────────────── Payload ──────────────────────→│←─ Signature ─→│

Header:  {"alg":"HS512"}                           → 算法声明
Payload: {"sub":"testuser","iat":1713000000,...}    → 用户名、签发时间、过期时间
Signature: HMAC-SHA512(Header + "." + Payload, secret) → 签名
```

**三个核心方法解析：**

```java
// ============ 生成 Token ============
public String generateToken(String username) {
    return Jwts.builder()
        .subject(username)                                          // subject = 用户名
        .issuedAt(new Date())                                       // iat = 当前时间
        .expiration(new Date(System.currentTimeMillis() + expiration)) // exp = 当前时间 + 24小时
        .signWith(getSigningKey())                                  // 使用 HS512 签名
        .compact();                                                  // 生成最终字符串
    // 返回: "eyJhbGciOiJIUzUxMiJ9.eyJzdWIiOiJ0ZXN0dXNlciIs..."
}

// ============ 解析 Token ============
public String getUsernameFromToken(String token) {
    return Jwts.parser()
        .verifyWith(getSigningKey())      // 设置签名验证密钥
        .build()
        .parseSignedClaims(token)         // 验证签名 + 解析 Payload
        .getPayload()
        .getSubject();                    // 提取 subject 字段 = 用户名
    // 如果签名不正确或 Token 过期，会抛出 JwtException
}

// ============ 验证 Token ============
public boolean validateToken(String token) {
    try {
        Jwts.parser()
            .verifyWith(getSigningKey())
            .build()
            .parseSignedClaims(token);    // 尝试解析
        return true;                       // 成功 → Token 有效
    } catch (JwtException | IllegalArgumentException e) {
        return false;                      // 失败 → Token 无效（过期/篡改/格式错误）
    }
}

// ============ 密钥生成 ============
private SecretKey getSigningKey() {
    return Keys.hmacShaKeyFor(secret.getBytes(StandardCharsets.UTF_8));
    // 将配置文件中的密钥字符串转为 SecretKey 对象
    // 密钥长度需 ≥ 64 字节（512 位）以满足 HS512 要求
}
```

---

#### 11.1.5 AuthController.java + AuthServiceImpl.java — 认证模块

**文件路径：**
- `backend/src/main/java/com/iras/controller/AuthController.java`
- `backend/src/main/java/com/iras/service/impl/AuthServiceImpl.java`

**Controller 层（请求入口）：**

```java
@RestController                    // 声明为 RESTful 控制器（返回 JSON 而非页面）
@RequestMapping("/api/auth")       // 所有接口路径前缀
@RequiredArgsConstructor           // Lombok 自动生成构造函数，注入 final 字段
public class AuthController {

    private final AuthService authService;  // 认证服务（由 Lombok 通过构造函数注入）

    @PostMapping("/register")              // POST /api/auth/register
    public Result<Map<String, Object>> register(@Valid @RequestBody RegisterRequest request) {
        // @Valid: 触发 RegisterRequest 上的 @NotBlank 等校验注解
        // @RequestBody: 将 JSON 请求体反序列化为 RegisterRequest 对象
        return authService.register(request);  // 委托给 Service 层处理
    }

    @PostMapping("/login")                 // POST /api/auth/login
    public Result<Map<String, Object>> login(@Valid @RequestBody LoginRequest request) {
        return authService.login(request);
    }
}
```

**Service 层（业务逻辑）— 注册流程：**

```java
@Override
public Result<Map<String, Object>> register(RegisterRequest request) {
    // 第一步：用户名唯一性检查
    if (userMapper.findByUsername(request.getUsername()) != null) {
        return Result.error(400, "用户名已存在");
    }

    // 第二步：邮箱唯一性检查（仅在提供了邮箱时检查）
    if (request.getEmail() != null && userMapper.findByEmail(request.getEmail()) != null) {
        return Result.error(400, "邮箱已被注册");
    }

    // 第三步：构建用户实体
    User user = new User();
    user.setUsername(request.getUsername());
    user.setPassword(passwordEncoder.encode(request.getPassword()));
    // ↑ BCrypt 加密：每次加密结果不同（内置随机盐），但 matches() 可正确比较
    user.setEmail(request.getEmail());

    // 第四步：插入数据库（MyBatis 自动生成的 INSERT 语句）
    userMapper.insert(user);
    // @Options(useGeneratedKeys = true, keyProperty = "id") 会将自增 ID 回填到 user.id

    // 第五步：生成 JWT Token
    String token = jwtUtil.generateToken(user.getUsername());

    // 第六步：组装返回数据
    Map<String, Object> data = new HashMap<>();
    data.put("token", token);         // 前端后续请求都携带此 Token
    data.put("username", user.getUsername());
    data.put("email", user.getEmail());

    return Result.success("注册成功", data);
}
```

**Service 层（业务逻辑）— 登录流程：**

```java
@Override
public Result<Map<String, Object>> login(LoginRequest request) {
    // 第一步：支持用户名或邮箱登录
    User user = userMapper.findByUsername(request.getUsername());
    if (user == null) {
        user = userMapper.findByEmail(request.getUsername());
        // ↑ 如果用户名查找失败，尝试用同一字段作为邮箱查找
        // 前端登录表单的 "用户名或邮箱" 字段复用
    }

    if (user == null) {
        return Result.error(401, "用户名或密码错误");
        // ↑ 安全考虑：不区分 "用户不存在" 和 "密码错误"，防止枚举攻击
    }

    // 第二步：BCrypt 密码验证
    if (!passwordEncoder.matches(request.getPassword(), user.getPassword())) {
        // passwordEncoder.matches(明文密码, 数据库中的BCrypt哈希)
        // 内部原理：从哈希中提取盐值，用盐值加密明文，比较结果
        return Result.error(401, "用户名或密码错误");
    }

    // 第三步：生成 JWT Token（登录成功）
    String token = jwtUtil.generateToken(user.getUsername());

    Map<String, Object> data = new HashMap<>();
    data.put("token", token);
    data.put("username", user.getUsername());
    data.put("email", user.getEmail());

    return Result.success("登录成功", data);
}
```

---

#### 11.1.6 JobController.java + JobServiceImpl.java — 职位模块

**Controller 层：**

```java
@RestController
@RequestMapping("/api/jobs")
@RequiredArgsConstructor
public class JobController {

    private final JobService jobService;

    @GetMapping("/search")    // GET /api/jobs/search?jobName=xxx&city=xxx&page=1&size=20
    public Result<PageResult<JobInfo>> searchJobs(JobSearchRequest request) {
        // Spring 自动将 Query 参数绑定到 JobSearchRequest 对象的同名字段
        // 例如 ?jobName=开发 → request.setJobName("开发")
        PageResult<JobInfo> result = jobService.searchJobs(request);
        return Result.success(result);
    }

    @GetMapping("/{id}")      // GET /api/jobs/1
    public Result<JobInfo> getJobById(@PathVariable Long id) {
        // @PathVariable 从 URL 路径中提取 {id} 参数
        JobInfo job = jobService.getJobById(id);
        if (job == null) {
            return Result.error(404, "职位不存在");
        }
        return Result.success(job);
    }
}
```

**Service 层 — 分页搜索实现：**

```java
@Override
public PageResult<JobInfo> searchJobs(JobSearchRequest request) {
    // 第一步：处理分页参数默认值
    int page = request.getPage() != null ? request.getPage() : 1;    // 默认第 1 页
    int size = request.getSize() != null ? request.getSize() : 20;   // 默认每页 20 条

    // 第二步：计算 SQL 分页偏移量
    int offset = (page - 1) * size;
    // 例如 page=2, size=20 → offset=20（跳过前 20 条）

    // 第三步：查询当前页数据
    List<JobInfo> records = jobMapper.searchJobs(
            request.getJobName(),    // 职位名称（可为 null）
            request.getCity(),       // 城市（可为 null）
            request.getSalaryMin(),  // 最低薪资（可为 null）
            request.getSalaryMax(),  // 最高薪资（可为 null）
            offset, size);

    // 第四步：查询总记录数（同样的搜索条件）
    long total = jobMapper.countSearchJobs(
            request.getJobName(), request.getCity(),
            request.getSalaryMin(), request.getSalaryMax());

    // 第五步：组装分页结果
    PageResult<JobInfo> result = new PageResult<>();
    result.setRecords(records);                                      // 当前页数据
    result.setTotal(total);                                          // 总记录数
    result.setPage(page);                                            // 当前页码
    result.setSize(size);                                            // 每页大小
    result.setTotalPages((int) Math.ceil((double) total / size));    // 总页数（向上取整）

    return result;
}
```

**Mapper 层 — MyBatis 动态 SQL（JobMapper.xml）解析：**

```xml
<select id="searchJobs" resultType="com.iras.entity.JobInfo">
    SELECT * FROM job_info
    <where>
        <!-- <where> 标签自动处理：如果内部有内容，自动添加 WHERE 关键字，
             并去除第一个 AND/OR 前缀 -->

        <!-- 条件 1：职位名称模糊匹配（仅在 jobName 非空时拼接） -->
        <if test="jobName != null and jobName != ''">
            AND job_name LIKE CONCAT('%', #{jobName}, '%')
            <!-- #{jobName} 使用参数化查询（PreparedStatement），防止 SQL 注入 -->
            <!-- LIKE '%关键词%' 实现模糊匹配 -->
        </if>

        <!-- 条件 2：城市模糊匹配 -->
        <if test="city != null and city != ''">
            AND city LIKE CONCAT('%', #{city}, '%')
        </if>

        <!-- 条件 3：最低薪资筛选 -->
        <if test="salaryMin != null and salaryMin != ''">
            AND CAST(REPLACE(REPLACE(salary, 'k', ''), 'K', '') AS UNSIGNED) &gt;= #{salaryMin}
            <!-- 薪资字段存储为字符串（如 "15000" 或 "15k"），需要：
                 1. REPLACE 去除 'k'/'K' 后缀
                 2. CAST 转为无符号整数
                 3. 进行数值比较
                 &gt;= 是 XML 转义的 >= 符号 -->
        </if>

        <!-- 条件 4：最高薪资筛选（同理） -->
        <if test="salaryMax != null and salaryMax != ''">
            AND CAST(REPLACE(REPLACE(salary, 'k', ''), 'K', '') AS UNSIGNED) &lt;= #{salaryMax}
        </if>
    </where>
    ORDER BY id DESC          <!-- 按 ID 降序（最新数据在前） -->
    LIMIT #{offset}, #{size}  <!-- MySQL 分页语法：从 offset 行开始取 size 行 -->
</select>
```

---

#### 11.1.7 DifyServiceImpl.java — AI 服务核心实现

**文件路径：** `backend/src/main/java/com/iras/service/impl/DifyServiceImpl.java`

**职责：** 通过 HTTP 连接调用 Dify AI 平台的 Workflow API，实现职能画像和简历诊断。

**整体调用架构：**

```
DifyServiceImpl
    ├── getJobProfile(jobName)       → 纯文本输入 → callDifyApi()
    └── diagnoseResume(file)         → 两步流程
         ├── uploadFile(file)        → 上传文件到 Dify → 获取 upload_file_id
         └── callDifyApi()           → 用文件 ID 调用 Workflow
```

**① 文件上传详解（uploadFile 方法）：**

```java
private String uploadFile(MultipartFile file) {
    // 使用手动拼接 multipart/form-data 请求体（未使用第三方 HTTP 库）
    String boundary = "----WebKitFormBoundary" + System.currentTimeMillis();
    // ↑ multipart 边界字符串，用于分隔不同字段

    HttpURLConnection connection = (HttpURLConnection) new URL(uploadUrl).openConnection();
    connection.setRequestMethod("POST");
    connection.setRequestProperty("Authorization", "Bearer " + resumeDiagnosisKey);
    connection.setRequestProperty("Content-Type", "multipart/form-data; boundary=" + boundary);
    connection.setDoOutput(true);     // POST 请求需要设置为 true
    connection.setConnectTimeout(60000);  // 连接超时 60 秒
    connection.setReadTimeout(60000);     // 读取超时 60 秒

    // 手动拼接 multipart 请求体
    OutputStream os = connection.getOutputStream();

    // 字段 1：file（文件内容）
    os.write(("--" + boundary + "\r\n").getBytes());                          // 字段开始边界
    os.write(("Content-Disposition: form-data; name=\"file\"; filename=\"" +
              file.getOriginalFilename() + "\"\r\n").getBytes());             // 字段元信息
    os.write(("Content-Type: " + file.getContentType() + "\r\n\r\n").getBytes()); // 文件 MIME 类型
    os.write(file.getBytes());                                                // 文件二进制内容
    os.write("\r\n".getBytes());                                              // 字段结束换行

    // 字段 2：user（用户标识）
    os.write(("--" + boundary + "\r\n").getBytes());
    os.write("Content-Disposition: form-data; name=\"user\"\r\n\r\n".getBytes());
    os.write("iras-user\r\n".getBytes());

    // 结束边界
    os.write(("--" + boundary + "--\r\n").getBytes());

    // 读取响应，解析 JSON 提取文件 ID
    JsonNode root = objectMapper.readTree(responseBody);
    return root.get("id").asText();  // 返回 Dify 平台分配的 upload_file_id
}
```

**② Workflow 请求体构建（文件引用模式）：**

```java
private String buildFileWorkflowRequest(String inputKey, String uploadFileId) {
    // 构建的 JSON 结构：
    // {
    //   "inputs": {
    //     "resume_text": [
    //       {
    //         "transfer_method": "local_file",   ← 使用平台内文件传输
    //         "upload_file_id": "file-xxx",       ← 上一步获取的文件 ID
    //         "type": "document"                  ← 文件类型
    //       }
    //     ]
    //   },
    //   "response_mode": "blocking",             ← 同步阻塞模式
    //   "user": "iras-user"                      ← 用户标识
    // }
}
```

**③ Dify API 调用通用方法（callDifyApi）：**

```java
private String callDifyApi(String urlStr, String requestBody, String apiKey) {
    HttpURLConnection connection = (HttpURLConnection) new URL(urlStr).openConnection();
    connection.setRequestMethod("POST");
    connection.setRequestProperty("Authorization", "Bearer " + apiKey);
    connection.setRequestProperty("Content-Type", "application/json");
    connection.setDoOutput(true);
    connection.setConnectTimeout(300000);  // 5 分钟超时（AI 推理耗时较长）
    connection.setReadTimeout(300000);

    // 写入请求体
    OutputStream os = connection.getOutputStream();
    os.write(requestBody.getBytes(StandardCharsets.UTF_8));

    // 读取响应
    int responseCode = connection.getResponseCode();
    InputStream inputStream = (responseCode >= 200 && responseCode < 300)
            ? connection.getInputStream()     // 成功响应
            : connection.getErrorStream();    // 错误响应
    String responseBody = new String(inputStream.readAllBytes(), StandardCharsets.UTF_8);

    if (responseCode >= 200 && responseCode < 300) {
        // 解析 Dify 响应结构：{ "data": { "outputs": { "result": "AI生成的内容" } } }
        JsonNode root = objectMapper.readTree(responseBody);
        JsonNode outputs = root.path("data").path("outputs");
        if (outputs.has("result")) {
            return outputs.get("result").asText();  // 提取 result 字段
        }
        return responseBody;  // 无 result 字段时返回完整响应
    }
}
```

**Dify 响应结构图：**

```json
{
  "workflow_run_id": "xxx",
  "task_id": "xxx",
  "data": {
    "outputs": {
      "result": "这里是 AI 生成的内容（JSON 字符串或纯文本）"
    },
    "elapsed_time": 120.5,
    "total_tokens": 5000
  }
}
```

---

#### 11.1.8 DTO 层详解

**Result.java — 统一响应体：**

```java
@Data @AllArgsConstructor @NoArgsConstructor
public class Result<T> {
    private int code;       // 业务状态码（200=成功, 400=参数错误, 401=未认证, 500=服务器错误）
    private String message; // 响应消息
    private T data;         // 响应数据（泛型，支持任意类型）

    // 工厂方法 - 成功
    public static <T> Result<T> success(T data) {
        return new Result<>(200, "success", data);
    }
    public static <T> Result<T> success(String message, T data) {
        return new Result<>(200, message, data);
    }

    // 工厂方法 - 失败
    public static <T> Result<T> error(int code, String message) {
        return new Result<>(code, message, null);
    }
    public static <T> Result<T> error(String message) {
        return new Result<>(500, message, null);  // 默认 500
    }
}
```

**PageResult.java — 分页结果封装：**

```java
@Data @AllArgsConstructor @NoArgsConstructor
public class PageResult<T> {
    private List<T> records;   // 当前页数据列表
    private long total;        // 总记录数
    private int page;          // 当前页码
    private int size;          // 每页大小
    private int totalPages;    // 总页数
}
```

**JobSearchRequest.java — 搜索请求参数：**

```java
@Data
public class JobSearchRequest {
    private String jobName;       // 职位名称（模糊匹配）
    private String city;          // 城市（模糊匹配）
    private String salaryMin;     // 最低薪资
    private String salaryMax;     // 最高薪资
    private Integer page = 1;     // 页码（默认 1）
    private Integer size = 20;    // 每页大小（默认 20）
    // 注意：使用 Integer 而非 int，因为需要判断是否为 null（未传参时使用默认值）
}
```

---

### 11.2 前端源码解析

---

#### 11.2.1 main.js — Vue 应用入口

**文件路径：** `frontend/src/main.js`

**职责：** 创建 Vue 应用实例，注册全局插件和组件。

```javascript
import { createApp } from 'vue'         // Vue 3 创建应用函数
import { createPinia } from 'pinia'     // Pinia 状态管理
import ElementPlus from 'element-plus'  // Element Plus UI 组件库
import 'element-plus/dist/index.css'    // Element Plus 全局样式
import zhCn from 'element-plus/dist/locale/zh-cn.mjs'  // 中文语言包
import * as ElementPlusIconsVue from '@element-plus/icons-vue'  // 所有图标
import App from './App.vue'             // 根组件
import router from './router'           // 路由配置

const app = createApp(App)  // 创建 Vue 应用实例

// 批量注册所有 Element Plus 图标组件
// 注册后可在任意组件中使用 <Edit />, <Search /> 等图标
for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
  app.component(key, component)
}

app.use(createPinia())     // 注册 Pinia 状态管理插件
app.use(router)            // 注册 Vue Router 路由插件
app.use(ElementPlus, { locale: zhCn })  // 注册 Element Plus（中文语言）
app.mount('#app')          // 挂载到 index.html 中的 #app 元素
```

---

#### 11.2.2 api/index.js — Axios API 封装

**文件路径：** `frontend/src/api/index.js`

**职责：** 创建统一的 HTTP 请求实例，封装请求/响应拦截器，导出各模块 API。

**① Axios 实例创建：**

```javascript
const api = axios.create({
  baseURL: '/iras',            // 请求前缀，与后端 context-path 一致
                                // 通过 Vite 代理转发到 http://localhost:8080/iras
  timeout: 300000,             // 5 分钟超时（适配 Dify AI 推理耗时）
  headers: { 'Content-Type': 'application/json' }
})
```

**② 请求拦截器（自动注入 Token）：**

```javascript
api.interceptors.request.use(config => {
  const token = localStorage.getItem('token')  // 从 localStorage 读取 Token
  if (token) {
    config.headers.Authorization = `Bearer ${token}`  // 添加到请求头
  }
  return config  // 返回修改后的配置
})
// 作用：所有请求自动携带 Token，无需手动添加
```

**③ 响应拦截器（统一错误处理）：**

```javascript
api.interceptors.response.use(
  response => {
    const { data } = response
    // 检查业务状态码（后端 Result.code）
    if (data.code && data.code !== 200) {
      ElMessage.error(data.message || '请求失败')  // 弹出错误提示
      return Promise.reject(data)                  // 抛出异常
    }
    return data  // 成功时返回数据体
  },
  error => {
    if (error.response?.status === 401) {
      // Token 过期或无效 → 清除登录状态 → 跳转登录页
      localStorage.removeItem('token')
      localStorage.removeItem('username')
      localStorage.removeItem('email')
      router.push('/login')
      ElMessage.error('登录已过期，请重新登录')
    } else {
      ElMessage.error(error.response?.data?.message || '网络错误')
    }
    return Promise.reject(error)
  }
)
```

**④ API 模块导出：**

```javascript
// 认证 API
export const authApi = {
  register: (data) => api.post('/api/auth/register', data),
  login: (data) => api.post('/api/auth/login', data)
}

// 职位 API
export const jobApi = {
  search: (params) => api.get('/api/jobs/search', { params }),
  getById: (id) => api.get(`/api/jobs/${id}`)
}

// Dify AI API
export const difyApi = {
  getJobProfile: (jobName) => api.post('/api/dify/job-profile', { job_name: jobName }),
  diagnoseResume: (file) => {
    const formData = new FormData()       // 文件上传需要使用 FormData
    formData.append('file', file)
    return api.post('/api/dify/diagnose', formData, {
      headers: { 'Content-Type': 'multipart/form-data' }  // 覆盖默认的 JSON 类型
    })
  }
}
```

---

#### 11.2.3 router/index.js — 路由配置与守卫

**文件路径：** `frontend/src/router/index.js`

**路由表解析：**

```javascript
const routes = [
  {
    path: '/login',                                    // 登录页
    component: () => import('../views/Login.vue'),     // 懒加载（按需加载，优化首屏）
    meta: { requiresAuth: false }                      // 无需认证
  },
  {
    path: '/',                                          // 主布局
    component: () => import('../views/Layout.vue'),
    redirect: '/jobs',                                  // 默认重定向到 /jobs
    meta: { requiresAuth: true },                       // 需要认证
    children: [                                         // 嵌套子路由
      { path: 'jobs',        component: () => import('../views/Jobs.vue'),        meta: { title: '职位检索' } },
      { path: 'job-profile', component: () => import('../views/JobProfile.vue'),  meta: { title: '职能画像' } },
      { path: 'diagnosis',   component: () => import('../views/Diagnosis.vue'),   meta: { title: '智能诊断' } }
    ]
  }
]
```

**路由守卫详解：**

```javascript
router.beforeEach((to, from, next) => {
  const userStore = useUserStore()  // 获取用户状态

  if (to.meta.requiresAuth !== false && !userStore.token) {
    // 情况 1：目标页面需要认证但用户未登录
    // 示例：访问 /jobs 但没有 Token → 跳转 /login
    next('/login')

  } else if (to.path === '/login' && userStore.token) {
    // 情况 2：用户已登录却访问登录页
    // 示例：已登录用户手动输入 /login → 跳转 /（首页）
    next('/')

  } else {
    // 情况 3：正常放行
    // 示例：已登录访问 /jobs，或未登录访问 /login
    next()
  }
})
```

---

#### 11.2.4 store/user.js — Pinia 状态管理

**文件路径：** `frontend/src/store/user.js`

**设计模式：** 组合式 API（Composition API）风格的 Pinia Store

```javascript
export const useUserStore = defineStore('user', () => {
  // ===== 状态定义 =====
  // 从 localStorage 初始化（页面刷新时恢复登录状态）
  const token = ref(localStorage.getItem('token') || '')
  const username = ref(localStorage.getItem('username') || '')
  const email = ref(localStorage.getItem('email') || '')

  // ===== 操作方法 =====
  function setUser(userData) {
    // 更新内存状态（响应式，自动触发视图更新）
    token.value = userData.token
    username.value = userData.username
    email.value = userData.email || ''
    // 持久化到 localStorage（页面刷新后可恢复）
    localStorage.setItem('token', userData.token)
    localStorage.setItem('username', userData.username)
    localStorage.setItem('email', userData.email || '')
  }

  function logout() {
    // 清除内存状态
    token.value = ''
    username.value = ''
    email.value = ''
    // 清除 localStorage
    localStorage.removeItem('token')
    localStorage.removeItem('username')
    localStorage.removeItem('email')
  }

  return { token, username, email, setUser, logout }
})
```

**数据持久化流程：**

```
登录成功 → setUser() → 同时写入 ref + localStorage
页面刷新 → Store 初始化 → 从 localStorage 读取 → 恢复登录状态
退出登录 → logout() → 同时清空 ref + localStorage
```

---

#### 11.2.5 Login.vue — 登录/注册页面

**文件路径：** `frontend/src/views/Login.vue`

**模板结构解析：**

```html
<template>
  <div class="login-container">          <!-- 全屏渐变紫色背景 -->
    <div class="login-card">             <!-- 白色居中卡片 -->
      <div class="login-header">         <!-- 标题区 -->
        <h1>智能简历诊断系统</h1>
        <p>Intelligent Resume Analysis System</p>
      </div>

      <el-tabs v-model="activeTab" stretch>  <!-- Tab 切换登录/注册 -->
        <el-tab-pane label="登录" name="login">
          <el-form :rules="loginRules" @keyup.enter="handleLogin">
            <!-- @keyup.enter: 按回车触发登录 -->
            <el-input v-model="loginForm.username" placeholder="用户名或邮箱" />
            <el-input v-model="loginForm.password" type="password" show-password />
            <!-- show-password: 显示密码可见性切换按钮 -->
            <el-button :loading="loading" @click="handleLogin">登 录</el-button>
            <!-- :loading: 加载中时显示旋转图标并禁用按钮 -->
          </el-form>
        </el-tab-pane>

        <el-tab-pane label="注册" name="register">
          <!-- 注册表单：用户名 + 邮箱（选填）+ 密码 + 确认密码 -->
        </el-tab-pane>
      </el-tabs>
    </div>
  </div>
</template>
```

**登录逻辑详解：**

```javascript
async function handleLogin() {
  // 第一步：触发表单验证
  try { await loginFormRef.value.validate() }
  catch { return }  // 验证不通过则终止

  loading.value = true  // 显示加载状态

  try {
    // 第二步：调用登录 API
    const res = await authApi.login(loginForm)
    // 返回: { code: 200, data: { token, username, email } }

    // 第三步：保存用户信息到 Pinia Store + localStorage
    userStore.setUser(res.data)

    ElMessage.success('登录成功')

    // 第四步：跳转到首页
    router.push('/')
  } catch (e) {
    // 错误已在 Axios 拦截器中统一处理（弹出 ElMessage.error）
  } finally {
    loading.value = false  // 隐藏加载状态
  }
}
```

**注册表单验证规则：**

```javascript
const registerRules = {
  username: [
    { required: true, message: '请输入用户名', trigger: 'blur' },
    { min: 2, max: 20, message: '用户名长度 2-20 个字符', trigger: 'blur' }
  ],
  password: [
    { required: true, message: '请输入密码', trigger: 'blur' },
    { min: 6, message: '密码长度不少于 6 位', trigger: 'blur' }
  ],
  confirmPassword: [
    { required: true, message: '请确认密码', trigger: 'blur' },
    {
      validator: (rule, value, callback) => {
        if (value !== registerForm.password) {
          callback(new Error('两次输入的密码不一致'))  // 自定义校验：确认密码必须一致
        } else {
          callback()  // 校验通过
        }
      },
      trigger: 'blur'
    }
  ]
}
```

---

#### 11.2.6 Layout.vue — 主布局

**文件路径：** `frontend/src/views/Layout.vue`

**布局结构解析：**

```html
<el-container class="layout-container">
  <!-- 左侧边栏 -->
  <el-aside :width="isCollapse ? '64px' : '220px'">
    <!-- :width 动态宽度：折叠 64px / 展开 220px -->
    <div class="aside-header" @click="isCollapse = !isCollapse">
      <!-- 点击标题切换折叠状态 -->
      <span class="aside-title">IRAS</span>
    </div>

    <el-menu router :collapse="isCollapse"
             background-color="#1e293b" text-color="#94a3b8"
             active-text-color="#60a5fa">
      <!-- router 模式：点击菜单项自动跳转到 index 对应的路由 -->
      <el-menu-item index="/jobs">职位检索</el-menu-item>
      <el-menu-item index="/job-profile">职能画像</el-menu-item>
      <el-menu-item index="/diagnosis">智能诊断</el-menu-item>
    </el-menu>
  </el-aside>

  <!-- 右侧内容区 -->
  <el-container>
    <el-header>
      <!-- 面包屑导航 -->
      <el-breadcrumb separator="/">
        <el-breadcrumb-item :to="{ path: '/' }">首页</el-breadcrumb-item>
        <el-breadcrumb-item>{{ $route.meta.title }}</el-breadcrumb-item>
        <!-- $route.meta.title 来自路由配置中的 meta.title -->
      </el-breadcrumb>

      <!-- 用户下拉菜单 -->
      <el-dropdown @command="handleCommand">
        <span>{{ userStore.username }}</span>
        <template #dropdown>
          <el-dropdown-menu>
            <el-dropdown-item command="logout">退出登录</el-dropdown-item>
          </el-dropdown-menu>
        </template>
      </el-dropdown>
    </el-header>

    <el-main>
      <!-- 使用 keep-alive 缓存已访问的路由组件 -->
      <router-view v-slot="{ Component }">
        <keep-alive>
          <component :is="Component" />
          <!-- keep-alive 作用：切换路由时保留组件状态，
               避免重复渲染（如 Jobs 页面的搜索结果不会丢失） -->
        </keep-alive>
      </router-view>
    </el-main>
  </el-container>
</el-container>
```

---

#### 11.2.7 Jobs.vue — 职位检索页面

**文件路径：** `frontend/src/views/Jobs.vue`

**数据流：**

```
页面加载 → onMounted → fetchJobs() → jobApi.search() → 渲染 el-table
搜索按钮 → handleSearch() → page=1 → fetchJobs()     → 重新查询第 1 页
重置按钮 → handleReset() → 清空表单 → fetchJobs()     → 查询全部
分页切换 → fetchJobs()                                 → 查询对应页
```

**核心逻辑：**

```javascript
const searchForm = reactive({
  jobName: '',      // 职位名称
  city: '',         // 城市
  salaryMin: '',    // 最低薪资
  salaryMax: '',    // 最高薪资
  page: 1,          // 当前页码
  size: 20          // 每页大小
})

async function fetchJobs() {
  loading.value = true
  const res = await jobApi.search(searchForm)
  // searchForm 中的所有字段会作为 Query 参数拼接到 URL
  // 例: GET /iras/api/jobs/search?jobName=开发&city=北京&page=1&size=20
  jobList.value = res.data.records  // 当前页数据
  total.value = res.data.total      // 总记录数
}

function handleSearch() {
  searchForm.page = 1  // 搜索时重置到第 1 页
  fetchJobs()
}

function handleReset() {
  searchForm.jobName = ''
  searchForm.city = ''
  searchForm.salaryMin = ''
  searchForm.salaryMax = ''
  searchForm.page = 1
  fetchJobs()
}
```

**表格组件解析：**

```html
<el-table :data="jobList" stripe v-loading="loading">
  <!-- :data 绑定数据源 -->
  <!-- stripe 斑马纹样式 -->
  <!-- v-loading 加载遮罩 -->

  <el-table-column prop="jobName" label="职位名称" min-width="150" />
  <!-- prop 对应数据字段名，label 表头文本 -->

  <el-table-column prop="salary" label="薪资" width="120">
    <template #default="{ row }">
      <!-- 自定义列模板：{ row } 解构获取当前行数据 -->
      <span style="color: #f56c6c; font-weight: 600">{{ row.salary }}</span>
      <!-- 薪资红色高亮显示 -->
    </template>
  </el-table-column>

  <el-table-column label="操作" width="120" fixed="right">
    <!-- fixed="right" 固定在右侧 -->
    <template #default="{ row }">
      <el-button type="primary" link @click="viewDetail(row)">查看详情</el-button>
    </template>
  </el-table-column>
</el-table>

<!-- 分页组件 -->
<el-pagination
  v-model:current-page="searchForm.page"    // 双向绑定当前页码
  v-model:page-size="searchForm.size"        // 双向绑定每页大小
  :total="total"                             // 总记录数
  :page-sizes="[20, 50, 100]"               // 可选的每页大小
  layout="total, sizes, prev, pager, next, jumper"
  @current-change="fetchJobs"               // 页码变化时重新查询
  @size-change="fetchJobs"                   // 每页大小变化时重新查询
/>
```

---

#### 11.2.8 JobProfile.vue — 职能画像页面

**文件路径：** `frontend/src/views/JobProfile.vue`

**核心逻辑：**

```javascript
const jobName = ref('')         // 输入的职位名称
const loading = ref(false)      // 加载状态
const profileData = ref(null)   // AI 返回的画像数据

async function analyze() {
  if (!jobName.value.trim()) {
    ElMessage.warning('请输入职位名称')
    return
  }

  loading.value = true
  profileData.value = null

  const res = await difyApi.getJobProfile(jobName.value.trim())
  // 发送: POST /iras/api/dify/job-profile { "job_name": "软件工程师" }
  // 响应: { code: 200, data: "{\"job_title\":\"...\",\"hard_skills\":[...],...}" }

  // 解析 AI 返回的 JSON 字符串
  const data = typeof res.data === 'string' ? JSON.parse(res.data) : res.data
  // ↑ 如果 data 是字符串则 JSON.parse，否则直接使用

  profileData.value = data
  // data 结构: {
  //   job_title: "软件工程师",
  //   hard_skills: ["Java", "Spring", "MySQL", ...],
  //   soft_skills: ["沟通能力", "团队协作", ...],
  //   tools: ["IDEA", "Git", "Maven", ...],
  //   experience: "3-5年",
  //   education: "本科及以上"
  // }
}
```

**结果渲染（四宫格卡片）：**

```html
<el-row :gutter="20">  <!-- gutter: 卡片间距 -->
  <!-- 左上：硬技能 -->
  <el-col :span="12">  <!-- span=12: 占一半宽度（24栅格系统） -->
    <el-card>
      <ul>
        <li v-for="(skill, i) in profileData.hard_skills" :key="i">{{ skill }}</li>
      </ul>
    </el-card>
  </el-col>

  <!-- 右上：软技能 -->
  <el-col :span="12">
    <el-card>
      <ul>
        <li v-for="(skill, i) in profileData.soft_skills" :key="i">{{ skill }}</li>
      </ul>
    </el-card>
  </el-col>
</el-row>

<el-row :gutter="20" style="margin-top: 20px">
  <!-- 左下：工具（Tag 标签形式） -->
  <el-col :span="12">
    <el-card>
      <el-tag v-for="(tool, i) in profileData.tools" :key="i">{{ tool }}</el-tag>
    </el-card>
  </el-col>

  <!-- 右下：经验与学历 -->
  <el-col :span="12">
    <el-card>
      <p>{{ profileData.experience }}</p>
      <p>{{ profileData.education }}</p>
    </el-card>
  </el-col>
</el-row>
```

---

#### 11.2.9 Diagnosis.vue — 智能诊断页面

**文件路径：** `frontend/src/views/Diagnosis.vue`

**核心逻辑 — 诊断结果解析：**

```javascript
async function diagnose() {
  loading.value = true

  const res = await difyApi.diagnoseResume(selectedFile.value)
  // 发送: POST /iras/api/dify/diagnose (multipart/form-data)
  // 响应: { code: 200, data: "AI返回的内容" }

  let data = res.data

  // ===== 第一步：提取 <think> 标签（AI 推理过程） =====
  const thinkMatch = data.match(/<think>([\s\S]*?)<\/think>/)
  // [\s\S]*? 匹配任意字符（包括换行），非贪婪模式
  if (thinkContent) {
    thinkContent.value = thinkMatch[1].trim()  // 提取标签内容
    data = data.replace(/<think>[\s\S]*?<\/think>/, '').trim()  // 从数据中移除标签
  }

  // ===== 第二步：尝试解析 JSON =====
  try {
    const parsed = JSON.parse(data)

    if (Array.isArray(parsed)) {
      // 情况 A：直接是数组 → 匹配结果列表
      // [{ matched_job, matched_score, matched_reason, gap_points, interview_advice }, ...]
      matches.value = parsed

    } else if (parsed.matches) {
      // 情况 B：对象中有 matches 字段
      matches.value = parsed.matches

    } else if (parsed.result) {
      // 情况 C：对象中有 result 字段（可能是嵌套的 JSON 字符串）
      const inner = parsed.result
      // 递归提取 <think> 标签
      const innerThink = inner.match(/<think>([\s\S]*?)<\/think>/)
      if (innerThink) thinkContent.value = innerThink[1].trim()

      const cleanJson = inner.replace(/<think>[\s\S]*?<\/think>/, '').trim()
      try {
        const innerParsed = JSON.parse(cleanJson)
        matches.value = Array.isArray(innerParsed) ? innerParsed : [innerParsed]
      } catch {
        rawResult.value = cleanJson  // 解析失败则作为纯文本处理
      }
    } else {
      matches.value = [parsed]  // 单个对象包装为数组
    }
  } catch {
    rawResult.value = data  // JSON 解析失败则作为纯文本/Markdown 处理
  }
}
```

**匹配分颜色编码：**

```javascript
function getScoreColor(score) {
  if (score >= 80) return '#67c23a'  // 绿色：高度匹配
  if (score >= 60) return '#e6a23c'  // 橙色：中等匹配
  return '#f56c6c'                   // 红色：低匹配
}
```

**匹配卡片渲染：**

```html
<div v-for="(match, index) in matches" :key="index" class="match-card">
  <el-card shadow="hover">
    <!-- 匹配头部：岗位名 + 匹配分 -->
    <div class="match-header">
      <div>
        <h3>{{ match.matched_job }}</h3>
        <el-tag>{{ match.type || '全职' }}</el-tag>
        <el-tag v-if="match.company">{{ match.company }}</el-tag>
      </div>
      <div class="match-score">
        <span :style="{ color: getScoreColor(match.matched_score) }">
          {{ match.matched_score }}%
        </span>
      </div>
    </div>

    <!-- 薪资 -->
    <div v-if="match.salary">薪资：{{ match.salary }}</div>

    <!-- 匹配原因 -->
    <h4>匹配原因</h4>
    <p>{{ match.matched_reason }}</p>

    <!-- 差距分析（列表） -->
    <h4>差距分析</h4>
    <ul>
      <li v-for="(gap, i) in match.gap_points" :key="i">{{ gap }}</li>
    </ul>

    <!-- 面试建议（列表） -->
    <h4>面试建议</h4>
    <ul>
      <li v-for="(advice, i) in match.interview_advice" :key="i">{{ advice }}</li>
    </ul>
  </el-card>
</div>
```

---

### 11.3 完整请求链路源码追踪

以"智能诊断"功能为例，追踪一个请求从前端到后端再到 Dify 的完整代码执行路径：

```
┌─────────────────────────────────────────────────────────────────────────┐
│ Step 1: 用户点击"开始诊断"按钮 (Diagnosis.vue)                           │
│   @click="diagnose()" → diagnose() 函数被调用                           │
├─────────────────────────────────────────────────────────────────────────┤
│ Step 2: 调用 API (api/index.js)                                         │
│   difyApi.diagnoseResume(file)                                          │
│   → const formData = new FormData(); formData.append('file', file)      │
│   → api.post('/api/dify/diagnose', formData, {                         │
│       headers: { 'Content-Type': 'multipart/form-data' }               │
│     })                                                                  │
├─────────────────────────────────────────────────────────────────────────┤
│ Step 3: 请求拦截器注入 Token (api/index.js)                              │
│   config.headers.Authorization = `Bearer ${localStorage.getItem('token')}`│
├─────────────────────────────────────────────────────────────────────────┤
│ Step 4: Vite 代理转发 (vite.config.js)                                   │
│   /iras/api/dify/diagnose → http://localhost:8080/iras/api/dify/diagnose│
├─────────────────────────────────────────────────────────────────────────┤
│ Step 5: Spring Security 过滤器链 (SecurityConfig.java)                   │
│   JwtAuthenticationFilter.doFilterInternal()                            │
│   → extractToken(request) → "Bearer eyJ..." → "eyJ..."                 │
│   → jwtUtil.validateToken(token) → true                                 │
│   → jwtUtil.getUsernameFromToken(token) → "testuser"                    │
│   → SecurityContextHolder.getContext().setAuthentication(...)           │
│   → 权限检查: /api/dify/** 需要认证 → 已认证 → 放行                      │
├─────────────────────────────────────────────────────────────────────────┤
│ Step 6: DifyController.diagnoseResume() (DifyController.java)           │
│   → @RequestParam("file") MultipartFile file 接收上传文件                │
│   → difyService.diagnoseResume(file)                                    │
├─────────────────────────────────────────────────────────────────────────┤
│ Step 7: DifyServiceImpl.diagnoseResume() (DifyServiceImpl.java)         │
│   ┌────────────────────────────────────────────────────────────────┐    │
│   │ 7a. uploadFile(file)                                           │    │
│   │   → POST https://api.dify.ai/v1/files/upload                   │    │
│   │   → multipart/form-data (file + user)                          │    │
│   │   → 响应: { "id": "file-abc123" }                              │    │
│   └────────────────────────────────────────────────────────────────┘    │
│   ┌────────────────────────────────────────────────────────────────┐    │
│   │ 7b. buildFileWorkflowRequest("resume_text", "file-abc123")     │    │
│   │   → { inputs: { resume_text: [{transfer_method, upload_file_id}]}}│  │
│   └────────────────────────────────────────────────────────────────┘    │
│   ┌────────────────────────────────────────────────────────────────┐    │
│   │ 7c. callDifyApi(url, body, resumeDiagnosisKey)                 │    │
│   │   → POST https://api.dify.ai/v1/workflows/run                  │    │
│   │   → Header: Bearer app-eAFPOpK...                              │    │
│   │   → Body: { inputs: {...}, response_mode: "blocking", ... }    │    │
│   │   → 等待 AI 推理（约 3 分钟）                                   │    │
│   │   → 响应: { data: { outputs: { result: "..." } } }             │    │
│   │   → return outputs.result                                      │    │
│   └────────────────────────────────────────────────────────────────┘    │
├─────────────────────────────────────────────────────────────────────────┤
│ Step 8: DifyController 返回 Result.success(result)                      │
│   → { code: 200, message: "success", data: "AI返回的JSON/文本" }        │
├─────────────────────────────────────────────────────────────────────────┤
│ Step 9: 响应拦截器处理 (api/index.js)                                    │
│   data.code === 200 → 返回 data                                         │
├─────────────────────────────────────────────────────────────────────────┤
│ Step 10: Diagnosis.vue 处理结果                                          │
│   → 提取 <think> 标签                                                      │
│   → JSON.parse(data)                                                    │
│   → matches.value = parsed (结构化匹配结果)                              │
│   → 渲染匹配卡片列表                                                     │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 12. 附录

> 本章包含系统的补充信息，如请求链路、安全机制、错误码等。

### 12.1 完整请求链路示例

以"智能诊断"功能为例：

```
1. 用户在 Diagnosis.vue 上传简历文件
2. 点击"开始诊断" → difyApi.diagnoseResume(file)
3. Axios 发送 POST /iras/api/dify/diagnose (multipart/form-data)
4. Vite 代理转发到 http://localhost:8080/iras/api/dify/diagnose
5. Spring Security → JwtAuthenticationFilter 验证 Token
6. DifyController.diagnoseResume() 接收文件
7. DifyServiceImpl.diagnoseResume() 执行：
   a. uploadFile() → POST https://api.dify.ai/v1/files/upload → 获取 upload_file_id
   b. callDifyApi() → POST https://api.dify.ai/v1/workflows/run → 获取 AI 结果
8. DifyController 返回 Result.success(result)
9. Axios 响应拦截器处理 → 返回数据给组件
10. Diagnosis.vue 解析结果（提取 <think> 标签 → JSON.parse → 渲染匹配卡片）
```

### 11.2 安全机制汇总

| 机制 | 实现 |
|------|------|
| 密码加密 | BCrypt（Spring Security PasswordEncoder） |
| 认证方式 | JWT Bearer Token（无状态） |
| Token 有效期 | 24 小时 |
| CSRF | 禁用（REST API + 无状态 Session） |
| CORS | 允许所有来源（开发环境） |
| 接口权限 | `/api/auth/**` 公开，其余需认证 |
| 文件上传限制 | 10MB |
| SQL 注入防护 | MyBatis 参数化查询（`#{}` 占位符） |

### 11.3 错误码对照表

| HTTP 状态码 | 业务码 | 说明 |
|------------|--------|------|
| 200 | 200 | 请求成功 |
| 400 | 400 | 参数错误（用户名已存在、邮箱已注册、参数为空） |
| 401 | 401 | 未认证（Token 缺失/过期/无效） |
| 404 | 404 | 资源不存在（职位不存在） |
| 500 | 500 | 服务器内部错误（AI 服务调用失败等） |
